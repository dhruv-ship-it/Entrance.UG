import { ArrowRight, Construction, GraduationCap } from 'lucide-react';
import { Link, Navigate } from 'react-router-dom';
import { Button } from '../components/ui/button';
import { useAuth, dashboardPath } from './auth/auth-context';

export const LandingPage = () => { const { user, isLoading } = useAuth(); if (!isLoading && user) return <Navigate to={dashboardPath(user)} replace />; return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_50%_0%,#e4efd7,transparent_40%),#f7f8f4] p-6"><section className="max-w-2xl text-center"><div className="mx-auto grid size-16 place-items-center rounded-3xl bg-moss-800 text-lime shadow-float"><GraduationCap size={31} /></div><p className="eyebrow mt-8">Entrance UG</p><h1 className="mt-3 text-4xl font-semibold tracking-tight sm:text-6xl">Preparation, made more deliberate.</h1><p className="mx-auto mt-5 max-w-xl text-base leading-7 text-stone-600">A focused workspace for entrance-exam preparation. The full public experience is being designed next.</p><div className="mt-8 flex justify-center gap-3"><Link to="/login"><Button>Sign in <ArrowRight size={16} /></Button></Link><Link to="/signup"><Button variant="outline">Create account</Button></Link></div></section></main>; };
export const RolePendingPage = () => <main className="grid min-h-screen place-items-center bg-canvas p-6"><section className="max-w-md rounded-4xl border bg-white p-9 text-center shadow-card"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-moss-100 text-moss-800"><Construction /></div><h1 className="mt-5 text-2xl font-semibold">This dashboard is next in line.</h1><p className="mt-3 text-sm leading-6 text-stone-500">The Student workspace is the first complete dashboard. Parent, Mentor and Admin access will connect here as those role-specific products are built.</p><Link to="/login" className="mt-7 inline-block"><Button variant="outline">Back to sign in</Button></Link></section></main>;
